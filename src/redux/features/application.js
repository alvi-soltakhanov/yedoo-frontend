const initialState = {
    loading: false,
    error: null,
}

const application = (state = initialState, action) =>{
    switch (action.type) {
        default:
    return state
    }    
}


export default application;
